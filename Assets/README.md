# Icon assets

`MacSpace-icon.svg` is the editable vector source for the current silver tile with a compact blue brush and sparkle. `MacSpace-icon.png` is its 1024-pixel RGBA render, and `MacSpace.icns` packages the 16–1024 pixel macOS icon sizes. The canvas outside the rounded tile is transparent. The earlier brush-only assets remain for reference.
